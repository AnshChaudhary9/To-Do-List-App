spring:
  data:
    mongodb:
      uri: ${MONGODB_URI}
      database: journaldb
      auto-index-creation: true
  main:
    allow-circular-references: true
  application:
    name: Journal App
server:
  port: ${SERVER_PORT}

weather:
  api:
    key: ${WEATHER_API_KEY}
